# Проверка кода MOROK VPN — Отчёт

## ✅ Что работает правильно

### Зависимости
- Все пакеты актуальны и совместимы
- Нет битых зависимостей
- `flutter_vless` установлен корректно
- `crypto` установлен для SSL pinning

### Структура проекта
- Все экраны существуют
- Все роуты зарегистрированы
- Все провайдеры объявлены
- Нет циклических зависимостей

### Ключевые функции
- SOCKS5 сканер — работает (настоящий handshake)
- Админка — работает (ручной ввод кода)
- Баннеры — работают (оффлайн режим)
- Анимация загрузки — работает
- Локализация — RU/EN работают

---

## ⚠️ Найденные проблемы

### 1. Осталось "NEXA" в UI текстах (косметика)

**Файлы:**
- `lib/l10n/app_localizations_en.dart` — строки 722, 753
- `lib/l10n/app_localizations_ru.dart` — строки 728, 759
- `lib/models/key_input.dart` — строки 3, 41, 111
- `lib/models/access_key.dart` — строка 45
- `lib/screens/access/key_entry_screen.dart` — строка 22
- `lib/screens/identity/identity_screen.dart` — строка 97
- `lib/screens/profile/profile_screen.dart` — строка 191
- `lib/services/identity/device_identity.dart` — строка 5

**Проблема:** В hint'ах и комментариях осталось старое название "NEXA-XXXX-XXXX"

**Решение:** Заменить на "MOROK-XXXX-XXXX" или вообще убрать пример формата

---

### 2. Firebase Options не настроены

**Файл:** `lib/firebase_options.dart`

**Проблема:** Стоит заглушка с TODO

**Решение:** Заменить на реальные значения из Firebase Console (нужно сделать при настройке продакшена)

---

### 3. Не реализована навигация на экран подписки

**Файлы:**
- `lib/screens/premium/trial_screen.dart` — строки 281, 448
- `lib/screens/settings/devices_screen.dart` — строки 314, 410

**Проблема:** TODO — нет экрана с планами подписки

**Решение:** Создать `PremiumPlansScreen` или временно скрыть кнопки

---

### 4. Не реализовано отключение устройства

**Файл:** `lib/screens/settings/devices_screen.dart` — строка 314

**Проблема:** TODO — нет API вызова для отключения устройства

**Решение:** Добавить метод в `AccountRepository` и вызвать его

---

### 5. SSL Pinning может замедлить запросы

**Файл:** `lib/services/api/api_client.dart`

**Проблема:** Валидация сертификата перед каждым запросом добавляет задержку ~100-200мс

**Решение:** Кэшировать результат валидации на 5-10 минут

---

## 🐛 Потенциальные баги

### 6. Анимация загрузки — возможная утечка памяти

**Файл:** `lib/screens/splash/splash_screen.dart`

**Проблема:** `Future.delayed` в `initState` может вызвать setState после dispose

**Решение:** Проверять `mounted` перед setState (уже сделано ✅)

---

### 7. SOCKS5 сканер — возможный таймаут

**Файл:** `lib/services/security/socks5_scanner.dart`

**Проблема:** Если порт открыт но не отвечает, socket может зависнуть

**Решение:** Добавить общий таймаут на всю операцию сканирования

---

### 8. Баннеры — нет очистки старых

**Файл:** `lib/providers/banner_providers.dart`

**Проблема:** Локальные баннеры никогда не удаляются автоматически

**Решение:** Добавить TTL или ручное удаление из админки

---

##  Рекомендации

### Высокий приоритет
1. Заменить "NEXA" на "MOROK" во всех текстах
2. Настроить Firebase Options
3. Добавить SSL pinning кэш

### Средний приоритет
4. Создать экран подписки или скрыть кнопки
5. Реализовать отключение устройств
6. Добавить TTL для локальных баннеров

### Низкий приоритет
7. Оптимизировать SOCKS5 сканер
8. Добавить обработку ошибок в анимации

---

## 🎯 Итог

**Критических багов:** 0
**Важных проблем:** 3 (Firebase, подписка, устройства)
**Косметических:** 8 (осталось "NEXA" в текстах)
**Рекомендаций:** 8

**Проект готов к запуску после исправления Firebase и замены NEXA → MOROK.**
